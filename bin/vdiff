#!/bin/bash

ARGS=()

while [[ $# -gt 0 ]]; do
    case "$1" in
        -L) shift 2;;
        -*) shift;;
        *)  ARGS+=("$1"); shift;;
    esac
done

#diff -ubI '^[[:blank:]]*\*' "${ARGS[@]}"
gvimdiff "${ARGS[@]}"

#!/bin/bash

for FILE; do
    if [[ -e $FILE.bak ]]; then
        mv -iv "$FILE.bak" "$FILE"
    elif [[ $FILE =~ .bak$ ]]; then
        mv -iv "$FILE" "${FILE%.bak}"
    else
        echo "$0: $FILE.bak: No such file or directory" >&2
    fi
done

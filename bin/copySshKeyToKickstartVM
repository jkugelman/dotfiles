#!/bin/bash

main() {
    if (($# != 1)); then
        echo "Usage: $0 HOSTNAME" >&2
        exit 1
    fi

    local host=$1

    ssh-keygen -R "$host"
    scp -q -o PubkeyAuthentication=no ~/.ssh/id_rsa.pub "$host":.ssh/authorized_keys
}

main "$@"

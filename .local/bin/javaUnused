#!/bin/bash

dirs=("${@:-.}")

find "${dirs[@]}" -name "*.java" -print0 | while read -d $'\0' file; do
    class=$(basename "$file" .java)

    if ! grep -Rwq --include='*.java' --exclude="$class.java" "$class" "${dirs[@]}"; then
        echo "$file"
    fi
done
